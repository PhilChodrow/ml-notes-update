# Setup 

Anaconda environment named `cs451`. 

Register jupyter kernel with name `cs451`:

```bash
conda activate cs451
python -m ipykernel install --user --name cs451 --display-name "Python (cs451)"
``` 